# Allow these dependencies to run install/build scripts (pnpm blocks by default).
# esbuild compiles its native binary used by Vite.
allowBuilds:
  esbuild: true
  "@tailwindcss/oxide": true
  # workerd is wrangler's Worker runtime; its build is required to deploy.
  workerd: true
  sharp: true
